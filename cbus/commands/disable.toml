version = 1
command = "users.disable"
program = "the8020/users/disable"
summary = "Disable a user"
description = "Disable a user and revoke current sessions."
usage = "<username>"
mutates_state = true
restart_behavior = "none"
[[examples]]
command = "users.disable alice"
